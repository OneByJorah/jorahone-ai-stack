# StackDeploy — server config
# Tailscale IP of this server
SERVER_IP=REPLACE_WITH_YOUR_TAILSCALE_IP

# Obsidian vault path on the host
OBSIDIAN_VAULT_PATH=/home/j1admin/ObsidianVault

# Services
HONCHO_TOKEN=REPLAC...OKEN
HONCHO_DB_PASSWORD=REPLAC...WORD
POSTGRES_PASSWORD=REPLAC...WORD

# Optional: local LLM add-on (only used if enabled in docker-compose.yml)
# MODEL_PATH=/models/Qwen3.5-9B-Coder-Q4_K_M.gguf
# CTX_SIZE=65536

# --- Browser-search: Camofox ---
# CAMOFOX_API_KEY=REPLACE_WITH_CAMOFOX_API_KEY
# CAMOFOX_ADMIN_KEY=REPLACE_WITH_CAMOFOX_ADMIN_KEY
